export default class Day {
    /**
     *
     * @param date
     * @param playlistNames {Array.<String>}
     */
    constructor(date, playlistNames) {
        this.date = date;
        this.playlistNames = playlistNames;
    }
}